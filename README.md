# Build & Deploy (AWS SAM)

## Prerequisites
- `sam` (AWS SAM CLI) installed
- AWS credentials configured (e.g. via `aws configure`)

## Install dependencies
```bash
npm ci
```

## Build
```bash
sam build
```

## Deploy
```bash
sam deploy
```

## (Optional) Run tests
```bash
npm test
```

